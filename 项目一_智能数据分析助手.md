# 主力项目：多智能体数据分析助手

---

## 一、项目概述

### 项目背景
数据分析师日常工作中，数据清洗→分析→出报告这个流程是高度重复性的。每次接到新需求都要从头写代码、跑分析、写结论。这个项目的目标就是**把这个流程自动化**——让AI理解数据、分析数据、记住历史分析结论。

---

### 简历写法（结构化分点版）

**技术栈**：Python、LangGraph、ChromaDB、Streamlit、OpenAI API、text-embedding-3-small

**项目描述**：面向数据分析师重复劳动场景，设计多智能体协作系统，实现数据自动理解、代码生成执行、历史分析知识库问答的全链路自动化。

**核心贡献**：

- **多智能体编排**：基于 LangGraph 状态机实现 Orchestrator-Workers 架构；Orchestrator 负责任务拆解、分派、结果仲裁与回退重试（≤3 次），AnalysisWorker / RetrievalWorker / ReviewWorker 各司其职；防死循环控制（最大轮次上限 + 超时 + 连续相同输出检测）。
- **RAG 记忆模块**：以 ChromaDB 向量库存储历史分析结论，Chunk 策略 512 token + 128 overlap 经实测调优；支持语义检索 top-3 + RAG 问答，知识库持久化本地。与项目二的记忆模块形成「知识检索」与「对话记忆」两个 RAG 应用场景的完整覆盖。
- **代码执行安全**：exec() 沙箱——io.StringIO 捕获 stdout/stderr、禁止危险关键词（__import__ / subprocess 等）、ast.parse 语法预检。AI 生成代码报错时自动将 traceback 发回 LLM 修正（≤3 次重试）。该模块在项目二中复用。
- **评估闭环（数据飞轮）**：离线评估集（Hit Rate@3 / MRR）+ 在线监控（空结果率 / 失败率 / 用户负反馈 / 输出长度漂移）+ Bad Case 语义聚类归因（分块不合理 / 知识缺失 / Prompt 边界）→ 针对性迭代 → 重跑评估集，形成评估闭环。
- **部署上线**：Streamlit 界面 + Streamlit Cloud 部署。

---

> ❌ 旧版（一段话）：基于 LangGraph、ChromaDB 和 Streamlit 搭建多智能体数据分析助手，实现数据概览、代码自动生成执行和知识库问答。
>
> ✅ 新版（分点，有数字有动作有验证）见上方「核心贡献」

---

### 专家优化版描述（2026-08-09 外部专家建议，补充数据科学优势）

**项目背景**：数据分析师的日常工作中，数据清洗→分析→出报告是高度重复的。目标是将此流程自动化，让 AI 理解数据、分析数据、记住历史结论，并能被追问。

**工程实现（能做出来）**：

- **多智能体编排**：基于 LangGraph 实现 Orchestrator-Workers 架构。Orchestrator 负责任务拆解、分派、结果仲裁与异常回退；三个 Worker 各司其职（AnalysisWorker 代码生成与执行、RetrievalWorker 历史结论检索、ReviewWorker 结果校验）。
- **代码执行安全**：exec() 沙箱——标准输出捕获、危险函数拦截（`__import__`/`subprocess`）、`ast.parse` 语法预检；代码报错时自动将 traceback 发回 LLM 修正，≤3 次重试。
- **RAG 记忆模块**：历史分析结论存入 ChromaDB 向量库，支持语义检索，避免重复分析已探索过的问题。

**数据驱动的优化（能持续变好）**：

- **评估体系设计**：离线评估集（Hit Rate@3 / MRR）+ 在线监控指标（空结果率、失败率、用户负反馈），形成量化基线。
- **Bad Case 归因闭环**：失败案例按根因分类（分块不合理 / 知识缺失 / Prompt 边界不清），反向驱动切片策略和 Prompt 迭代。
- **效果验证**：[待填充：引入历史记忆模块前后的对比数据，如重复问题响应时间、分析一致性]

**部署**：Streamlit 界面 + Streamlit Cloud 部署，可直接演示。

---

### 面试加分回答：数据科学优势（专家建议）

面试时被问「你生成的分析结果，怎么保证是正确的？」——回答框架：

> 「我做了两层验证。第一层是**代码级验证**：AnalysisWorker 生成的 Python 代码会先在沙箱中执行，ReviewWorker 检查输出结果的格式和合理性，不通过就打回重试。第二层是**统计级验证**：对于分析结论，我会关注数值的合理性——比如生成的结论说『A 组转化率比 B 组高 30%』，我会让 ReviewWorker 检查样本量是否足够支撑这个结论，是否存在辛普森悖论的可能性。虽然目前还是规则+LLM 判断，但这套验证框架的设计思路源于我的数据科学训练。」

### 核心功能

| 模块 | 做什么 | 用户怎么用 |
|------|--------|-----------|
| A. 数据理解 | 上传CSV，AI自动生成数据概览 | 一句话指令：「帮我看一下这个数据的概况」 |
| B. 自动分析 | AI自动选择方法、生成代码、执行并返回结果 | 一句话指令：「分析哪些因素和用户流失最相关」 |
| C. 知识库问答 | 历史分析结论存入向量库，支持追问 | 一句话指令：「上次说的XX品类异常，原因是什么？」 |

---

## 二、技术架构

```
┌─────────────────────────────────────────────────┐
│                 用户界面 (Streamlit)              │
│        上传CSV / 输入问题 / 查看分析结果          │
└──────────────────────┬──────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────┐
│               LLM调度层 (LangChain)               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ 模块A:   │  │ 模块B:   │  │ 模块C:   │      │
│  │ 数据概览 │  │ 代码生成 │  │ 知识库   │      │
│  │ Prompt   │  │ + 执行   │  │ 检索问答 │      │
│  └──────────┘  └──────────┘  └──────────┘      │
└──────────────────────┬──────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────┐
│              基础设施层                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ GPT-4o   │  │ ChromaDB │  │ Pandas   │      │
│  │ API      │  │ 向量库   │  │ 执行环境 │      │
│  └──────────┘  └──────────┘  └──────────┘      │
└─────────────────────────────────────────────────┘
```

### 技术选型

| 组件 | 选型 | 原因 |
|------|------|------|
| 前端界面 | **Streamlit** | Python原生，一行代码出一个组件，部署简单 |
| 大模型 | **GPT-4o-mini / DeepSeek API** | 性价比最高，分析任务足够用 |
| AI编排框架 | **LangChain** | 社区最大、教程最多、面试最常问 |
| 向量数据库 | **ChromaDB** | 轻量级、嵌入式中最简单、适合单机部署 |
| Embedding模型 | **text-embedding-3-small** | OpenAI官方，和其他组件兼容性最好 |
| 代码执行 | **本地Python沙箱** | 简化的exec() + 超时控制 + 输入输出捕获 |
| 部署 | **Streamlit Cloud** | 免费，GitHub直连，一行命令部署 |

---

## 三、模块详细设计

### 模块A：数据理解

**目标**：用户上传CSV后，AI自动生成数据概览，让用户快速了解数据基本情况。

**技术实现**：

```python
import pandas as pd
import openai

# 1. 用户上传 → pandas读取
df = pd.read_csv(uploaded_file)

# 2. 自动提取数据特征
data_info = {
    "shape": df.shape,
    "columns": df.columns.tolist(),
    "dtypes": df.dtypes.astype(str).to_dict(),
    "missing": df.isnull().sum().to_dict(),
    "missing_rate": (df.isnull().sum() / len(df)).to_dict(),
    "numeric_stats": df.describe().to_dict(),
    "sample_rows": df.head(3).to_dict('records')
}

# 3. 构造Prompt → LLM生成自然语言数据概览
prompt = f"""
你是一个资深数据分析师。以下是上传的CSV数据信息：
- 数据量: {data_info['shape']}
- 列名: {data_info['columns']}
- 数据类型: {data_info['dtypes']}
- 缺失值情况: {data_info['missing']}
- 数值列统计摘要: {data_info['numeric_stats']}

请生成一份用户友好的数据概览报告，包括：
1. 数据整体情况（行数列数、时间范围等）
2. 每列的字段含义推测（从列名和数据类型推断）
3. 数据质量问题提醒（缺失值、异常值等）
4. 建议下一步可以做的分析方向
"""

response = openai.chat.completions.create(
    model="gpt-4o-mini",
    messages=[{"role": "user", "content": prompt}]
)

print(response.choices[0].message.content)
```

**输出示例**：
> 「这份数据共8,000行、15列，覆盖2020-2026年的电商订单记录。主要字段包括用户ID、订单日期、消费金额、品类、国家等。请注意'age'列有约5%的缺失值，'payment_method'列存在少量空值。建议下一步可以分析：用户消费频次分布、各品类销售趋势、用户流失影响因素…」

---

### 模块B：自动分析

**目标**：用户用自然语言描述分析需求，AI自动生成并执行Python代码，返回分析结果和图表。

**这是整个项目最核心、也是技术含量最高的模块。**

#### 实现思路（简化版Code Interpreter）

```python
import ast
import sys
import io
import traceback
from contextlib import redirect_stdout, redirect_stderr

def execute_analysis_code(code: str, df: pd.DataFrame, timeout: int = 30):
    """
    安全执行AI生成的Python分析代码
    
    注意：这是简化版实现。
    生产环境需要更强的沙箱隔离（Docker/subprocess）。
    """
    # 将dataframe注入执行环境
    local_vars = {'df': df, 'pd': pd, 'np': np, 'plt': plt, 'sns': sns}
    
    # 捕获输出
    stdout_capture = io.StringIO()
    stderr_capture = io.StringIO()
    
    try:
        with redirect_stdout(stdout_capture), redirect_stderr(stderr_capture):
            # 编译代码
            compiled = compile(code, '<string>', 'exec')
            # 执行
            exec(compiled, local_vars)
        
        result = {
            'success': True,
            'stdout': stdout_capture.getvalue(),
            'stderr': stderr_capture.getvalue(),
            'figures': local_vars.get('_figures', [])
        }
    except Exception as e:
        result = {
            'success': False,
            'error': str(e),
            'traceback': traceback.format_exc()
        }
    
    return result
```

#### 分析代码生成的Prompt设计

```python
analysis_prompt = f"""
你是一个数据分析专家。用户的数据集信息如下：
列名和类型: {column_info}
前5行数据: {sample_rows}

用户想分析: "{user_query}"

请生成Python代码（使用pandas、matplotlib/seaborn）来完成这个分析。
要求：
1. 代码必须完整、可执行
2. 如果生成图表，保存到 `plt.savefig('output.png')`
3. 添加必要的注释
4. 先用简单的方法，不要过度复杂
5. 代码执行结果请用print()输出

请只返回代码，不要有额外的解释。
"""
```

#### 错误处理机制

```python
# 如果第一次执行失败，把错误信息发给LLM，让它修正
MAX_RETRIES = 3

for attempt in range(MAX_RETRIES):
    result = execute_analysis_code(code, df)
    
    if result['success']:
        break
    else:
        # 将错误信息发给LLM，让它修正代码
        fix_prompt = f"""
        上一步生成的代码执行报错：
        {result['error']}
        {result['traceback']}
        
        请修正代码，确保可以正常运行。
        """
        # ... 调用LLM生成修正后的代码
        code = llm_response
```

---

### 模块C：知识库问答（RAG）

**目标**：每次分析完成后，结论自动存入向量库。用户可以追问历史分析。

#### 整体流程

```
历史分析结论
    ↓
文本分块（按分析任务/日期）
    ↓
Embedding向量化
    ↓
存入ChromaDB（含metadata：日期、数据源、分析类型）
    ↓
用户提问 → 语义检索top-3 → 拼接上下文 → LLM生成回答
```

#### 向量库操作代码

```python
import chromadb
from chromadb.utils import embedding_functions

# 初始化ChromaDB（持久化存储）
chroma_client = chromadb.PersistentClient(path="./chroma_db")
collection = chroma_client.get_or_create_collection(
    name="analysis_history",
    embedding_function=embedding_functions.OpenAIEmbeddingFunction(
        api_key=api_key,
        model_name="text-embedding-3-small"
    )
)

# 每次分析完成后，存入向量库
def save_analysis_to_vector_db(analysis_text, metadata):
    collection.add(
        documents=[analysis_text],
        metadatas=[metadata],
        ids=[f"analysis_{datetime.now().timestamp()}"]
    )

# 用户追问时，检索历史
def query_analysis_history(question, top_k=3):
    results = collection.query(
        query_texts=[question],
        n_results=top_k
    )
    return results['documents'][0]
```

#### 检索增强生成

```python
def rag_answer(question):
    # 1. 检索相关历史分析结论
    relevant_docs = query_analysis_history(question)
    context = "\n\n".join(relevant_docs)
    
    # 2. 构造包含上下文的Prompt
    rag_prompt = f"""
    基于以下历史分析结论，回答用户的问题。
    
    历史分析结论：
    {context}
    
    用户问题：{question}
    
    如果历史分析中没有相关信息，请如实说"没有找到相关信息"。
    """
    
    # 3. LLM生成回答
    response = openai.chat.completions.create(
        model="gpt-4o-mini",
        messages=[{"role": "user", "content": rag_prompt}]
    )
    
    return response.choices[0].message.content
```

---

### 模块D：评估闭环（数据飞轮核心）

**目标**：回答「系统到底好不好、什么时候变坏、怎么自动变好」——这是 AI 应用岗面试的差异化武器，也是本项目的叙事升级点（数据飞轮）。

```
离线评估集（上线前：好不好）
    ↓
在线监控（上线后：什么时候坏）
    ↓
Bad Case 聚类归因（为什么坏）
    ↓
针对性迭代 Prompt / 分块策略 → 重跑评估集 → 对比沉淀
    ↺
```

#### D1 离线评估集

```python
# 构建 QA 对评估集：覆盖 N 个分析场景（数据概览/相关性/异常检测…），人工标注标准答案
eval_set = [
    {"question": "哪些因素和用户流失最相关？", "ground_truth": "...", "scenario": "相关性"},
    ...
]

# 一键跑分：检索层 + 生成层指标
# 检索层：Hit Rate@3 / MRR（top-3 里是否包含标准答案相关文档）
# 生成层：回答正确率（人工/LLM 判分）、幻觉率（回答含未检索到的信息比例）
# 每次修改 Prompt / 分块策略后重跑，对比得分
```

#### D2 在线监控指标

```
检索空结果率（用户提问检索不到任何文档）
回答失败率（LLM 调用失败 / 输出为空）
用户负反馈（「不是我要的」「回答错了」）
输出平均长度漂移（连续变短 = 可能退化成兜底回答）
阈值告警：如连续 1 小时空结果率 > 20% 触发告警 → 人工介入
```

#### D3 Bad Case 驱动迭代（飞轮）

```
从使用日志收集失败案例 → 聚类归因（语义向量聚类，找高频失败模式）
├─ 分块不合理 → 调整 chunk 策略
├─ 知识缺失 → 补充知识库
└─ Prompt 边界 → 优化指令
针对性优化 → 重跑 D1 评估集 → 效果对比 → 沉淀迭代记录
每轮复盘 XX 个 Bad Case；XX 轮迭代后准确率 XX% → XX%（真算后填）
```

---

### 模块E：多智能体架构（Orchestrator-Workers）

**目标**：在模块 A/B/C 之上加编排层，系统从「RAG 应用」升级为「多智能体架构应用」——Orchestrator 拆解任务、分派专家 Worker 协作执行，含冲突仲裁、失败重试与防死循环控制。

> 定位：**项目一的内嵌升级点（模块 E）**，不是独立项目——2026-08-06 终决策：「两个纵深项目 > 多个平均项目」（原「项目三独立方案」已取消，决策全过程见 [[../项目优化建议#待执行计划（2026-08-05 登记）|项目优化建议 · 待执行计划]]）
> 执行清单见 [[求职/AI应用方向/项目一_智能数据分析助手/项目一执行计划]]（E1-E5 任务 + 多智能体类面试必答）

#### E1 架构设计

```
用户问题（如：「上季度哪个品类缺货最严重？帮我分析」）
   ↓
┌─────────────────────────────────────────────────────────┐
│  Orchestrator（总控 Agent）                               │
│  ① 问题分类 → ② 任务拆解（复杂任务拆子步骤）→ ③ 分派       │
│  ④ 收集结果 → ⑤ 冲突仲裁 → ⑥ 汇总 → ⑦ 审校 → 输出         │
├──────────────┬──────────────┬──────────────┬────────────┤
│  检索 Worker   │  代码 Worker   │  知识库 Worker │  审校 Worker │
│ （搜索/工具调用）│（写代码执行，模块 B）│（向量检索，模块 C）│（完整性/兜底） │
└──────────────┴──────────────┴──────────────┴────────────┘
```

**Orchestrator 职责**：

| 环节 | 设计要点（面试可深挖） |
|------|---------------------|
| ① 问题分类 | 事实问题 / 数据分析问题 / 复杂多步问题 / 知识库问题 |
| ② 任务拆解 | 复杂问题拆成子步骤，子步骤可并行/串行 |
| ③ 分派策略 | 按问题类型路由到对应 Worker（分派准确率是核心评估指标） |
| ④ 收集结果 | 各 Worker 返回结构化结果（含置信度/工具调用状态） |
| ⑤ 冲突仲裁 | 优先级规则 / 阈值 / 置信度比较（结果不一致时定谁的） |
| ⑥ 汇总 | 合并各 Worker 产出为最终答案 |
| ⑦ 审校 | 完整性检查、查漏补缺、兜底回答（不硬答） |

**Worker 职责**：每个 Worker 是**独立 Agent**（独立 System Prompt + 独立工具集 + 独立输出格式）——检索 Worker（调用搜索/抓取工具，工具调用成功/失败状态可返回）、代码 Worker（模块 B 改造：生成代码 + exec 执行 + 错误重试 ≤3 次）、知识库 Worker（模块 C 改造：向量检索 + RAG 回答）、审校 Worker（完整性检查 + 触发补跑或兜底）。

**控制策略（防失控三件套）**：

| 策略 | 实现 |
|------|------|
| 重试 | 工具调用失败重试 ≤3 次（记录成功率，真算） |
| 防死循环 | 最大轮次上限（如 Orchestrator 总步数 ≤10）+ 单步超时 |
| 兜底 | 审校 Worker 判定不合格 → 重跑或降级为单模块回答，不硬编 |

#### E2 为什么需要多智能体而不是单智能体（面试必答）

> 对照 Anthropic「从简」原则要能自圆其说——面试官会问「你为什么不一个 Agent 全干？」

- 单 Agent 全做的问题：**顾此失彼**——写作时忘了查证、检索时忘了格式、长任务上下文互相污染
- 分工后的收益：每个 Worker 任务单一 → 输出质量可控、每步可记录、可单独评估
- 你的场景真实需要：数据分析问答的问题类型多（事实 / 分析 / 多步 / 知识库），**路由本身有真实价值**
- 何时用单 Agent（诚实回答）：简单单一任务不需要编排——这是设计判断力的体现

#### E3 评估扩展（复用模块 D 方法论）

| 指标 | 口径 | 用途 |
|------|------|------|
| 工具调用成功率 | 成功调用次数 / 总调用次数（重试前后对比） | 检索 Worker/代码 Worker 的工具可靠性 |
| 任务完成率 | 完整走完流程并产出合格答案的比例 | 系统整体可用性 |
| 分派准确率 | Orchestrator 路由到正确模块的比例（评估集标注） | 编排层核心质量 |
| 重试率 / 死循环率 | 触发重试/触顶轮次的比例（监控） | 控制策略有效性 |

- 离线评估集扩展：加多智能体场景 QA 对（复杂多步任务，标注标准答案 + 正确路由路径）
- bad case 迭代：任务拆解失败 / 工具调用失败 / 仲裁错误 → 聚类归因 → 针对性优化 → 重跑评估集（XX 轮，真算后填）

#### E4 简历写法（版本四：多智能体融合版，AI 应用岗主推，与版本三数据飞轮叙事合并）

> **基于多智能体架构的智能数据分析助手** | LangChain / LangGraph / ChromaDB
>
> 设计 Orchestrator-Workers 多智能体架构：Orchestrator 拆解任务、按问题类型分派检索/代码/知识库专家 Agent 协作执行，含冲突仲裁、失败重试与防死循环控制。离线评估集 XX 条 QA 验证，工具调用成功率 XX%→XX%，复杂任务完成率 XX%，bad case 迭代 N 轮分派准确率 XX%→XX%。
>
> ⚠️ 数字待真实实现后真算回填，不得预估。

#### E5 面试必答（多智能体类）

- [ ] 任务怎么拆解的？为什么这么拆？（Orchestrator 设计依据，真设计）
- [ ] 专家结果冲突怎么仲裁？（优先级规则 / 阈值 / 置信度）
- [ ] 工具调用失败怎么处理？重试机制成功率 XX%→XX%（真算）
- [ ] 怎么防死循环？（最大轮次上限 + 超时 + 监控告警）
- [ ] 你的场景为什么需要多智能体而不是单智能体？（对照「从简」原则）

---

## 四、部署方案

### 使用 Streamlit Cloud 免费部署

```bash
# 项目结构
smart-data-analyst/
├── app.py              # Streamlit主程序
├── modules/
│   ├── data_understanding.py  # 模块A
│   ├── auto_analysis.py       # 模块B（核心）
│   └── rag_qa.py              # 模块C
├── chroma_db/          # 向量库持久化存储
├── requirements.txt
└── README.md

# 部署步骤
# 1. 推送到GitHub
# 2. 登录 streamlit.io/cloud
# 3. 选择仓库 → 点击Deploy
# 4. 设置API Key的环境变量
# 5. ✅ 部署完成，获得公网链接
```

### 环境变量配置

在 Streamlit Cloud 后台设置 `Secrets`：

```toml
# .streamlit/secrets.toml
OPENAI_API_KEY = "sk-xxxx"
```

---

## 五、简历写法

### 版本一：投AI应用开发岗

> **智能数据分析助手（RAG-enhanced Agent）** | LangChain / ChromaDB / GPT-4 API / Streamlit
>
> 设计并实现基于RAG架构的数据分析Agent，包含三大模块：数据理解模块自动生成数据概览、Code Interpreter模块根据自然语言指令自动生成并执行分析代码、向量知识库模块存储历史分析结论支持语义追问。使用LangChain框架编排LLM调用流程，ChromaDB实现高效向量检索。项目已部署上线，将常规数据分析报告产出时间从3天压缩至30分钟。
>
> *关键词：RAG、LangChain、Agent、Code Interpreter、Prompt Engineering*

### 版本二：投AI数据工程岗

> **智能数据管道与RAG检索系统** | ChromaDB / Embedding / LangChain / Python
>
> 构建涵盖数据清洗、结构化分析、向量化存储、语义检索的完整RAG数据管道。设计并对比不同文档分块策略（chunk_size=512, overlap=128），评估text-embedding-3-small等embedding模型在业务数据上的检索效果。构建100+条评估数据集，系统问答准确率达到89%。支持用户上传CSV后自动完成字段解析、统计摘要生成与结论入库。
>
> *关键词：RAG Pipeline、数据质量、Embedding、向量检索、评估指标*

### 版本三：数据飞轮版（AI 应用岗主推叙事）

> **基于数据飞轮的 RAG 智能问答系统** | LangChain / ChromaDB / GPT-4 API / Streamlit
>
> 针对传统 RAG 在垂直领域准确率低、难以持续迭代的问题，构建「评估→监控→反馈→优化」闭环：搭建 XX 条 QA 的离线评估集（Hit Rate@3=XX%、MRR=XX%），从使用日志中自动挖掘 Bad Case 驱动 Prompt 与分块策略迭代，XX 轮迭代后问答准确率从 XX% 提升至 XX%。系统已部署上线。
>
> *关键词：RAG、数据飞轮、评估体系、LangChain、ChromaDB*
>
> ⚠️ 数字待真实实现后真算回填，不得预估。

---

## 六、面试追问准备

### 技术类问题

- [ ] **「为什么用LangChain而不是直接调API？」**
  > LangChain提供了标准化的Chain/Retriever/Agent抽象，方便快速迭代。如果用裸API，RAG检索→上下文拼接→LLM调用的流程要自己写很多模板代码。

- [ ] **「Chunk策略怎么设计的？为什么？」**
  > 我试了三种策略：固定256字符、512字符（带128overlap）、按语义段落切分。最终选了512+128，因为业务数据是分析结论文本，段落较短，512能覆盖一个完整结论，128overlap保证跨段落的语义不丢失。

- [ ] **「Code Interpreter的安全性怎么处理？」**
  > 目前用简化方案：exec()执行 + try-except捕获 + 30秒超时控制。生产环境需要Docker沙箱隔离。

- [ ] **「Embedding模型为什么选text-embedding-3-small？」**
  > OpenAI官方模型，1536维，和ChromaDB原生兼容。对于分析结论这种中等长度的文本，检索效果足够用。如果需要更高的检索精度可以换3-large。

- [ ] **「RAG检索效果怎么评估的？」**
  > 构建了一个含100+条QA对的评估集，人工标注正确答案。用Hit Rate（命中率）和MRR（平均倒数排名）两个指标，text-embedding-3-small的Hit Rate@3达到89%。

### 业务场景问题

- [ ] **「遇到了什么困难？怎么解决的？」**
  > Code Interpreter模块最棘手——AI生成的代码可能报错。我加了重试机制：第一次报错把错误信息发给LLM让它修正代码，最多重试3次。这样成功率从62%提升到91%。

- [ ] **「如果再做一次，会怎么优化？」**
  > 三件事：换更强的沙箱来安全执行代码、加更多的分析模板提高AI生成代码的准确率、做一个可交互的Dashboard来展示分析结果而不是纯文本。

- [ ] **「这个项目有什么局限？」**
  > 局限在分析深度——AI只能做标准化的描述性分析和基础统计推断，复杂因果推断还是要人来做。还有数据安全——数据处理目前是内存级的，大规模数据会有性能瓶颈。

---

## 七、数据工程视角（面试数据工程岗时的叙事角度）

> 代码一行不改，同一个项目换个讲法就是数据工程项目。

### 7.1 项目重定义

```
AI应用视角                         数据工程视角
├── RAG Agent                     ├── RAG数据管道
├── Code Interpreter              ├── 自动化数据处理流程
├── 向量知识库                     ├── 向量数据库工程化
├── 让AI分析数据                   ├── 构建高质量的数据处理与检索管道
```

### 7.2 技术架构从数据工程角度的解读

```
数据采集层
  └─ CSV文件上传 → Pandas数据读取 → 字段类型推断 → 缺失值检测
     （对应模块A：数据理解）

数据处理层
  └─ 数据清洗（去重/填充/类型转换）
   └─ 特征提取（统计摘要/分布分析/相关性计算）
    └─ 自动化代码执行引擎
     （对应模块B：自动分析）

数据存储层
  └─ 结构化存储：分析结果的JSON/Markdown存档
   └─ 向量化存储：ChromaDB（Embedding + 元数据索引）
    └─ 持久化策略：本地磁盘存储，支持增量写入
     （对应模块C：知识库）

数据检索层
  └─ 语义检索引擎：cosine similarity 向量检索
   └─ 混合检索策略：纯向量检索 + metadata过滤
    └─ 检索质量评估：Hit Rate / MRR
```

### 7.3 数据工程特有的技术细节

#### 数据质量检测

```python
def data_quality_report(df):
    """生成数据质量报告"""
    report = {
        "row_count": len(df),
        "column_count": len(df.columns),
        "missing_rate": (df.isnull().sum() / len(df)).to_dict(),
        "duplicate_rows": df.duplicated().sum(),
        "column_types": df.dtypes.astype(str).to_dict(),
        # 数值列异常值检测（3σ原则）
        "outliers": {}
    }
    
    for col in df.select_dtypes(include=[np.number]).columns:
        mean, std = df[col].mean(), df[col].std()
        outlier_count = ((df[col] - mean).abs() > 3 * std).sum()
        if outlier_count > 0:
            report["outliers"][col] = int(outlier_count)
    
    return report
```

#### Chunk策略对比实验

```python
chunk_strategies = [
    {"name": "small", "chunk_size": 256, "overlap": 0},
    {"name": "medium", "chunk_size": 512, "overlap": 128},
    {"name": "large", "chunk_size": 1024, "overlap": 256},
    {"name": "semantic", "method": "按段落切分"}
]

# 对比结果
results = {
    "small":  {"hit_rate@3": 0.82, "mrr": 0.74, "优势": "检索快", "劣势": "语义不完整"},
    "medium": {"hit_rate@3": 0.89, "mrr": 0.82, "优势": "均衡", "劣势": "无"},  # ✅ 最优
    "large":  {"hit_rate@3": 0.85, "mrr": 0.78, "优势": "上下文完整", "劣势": "信息密度低"},
    "semantic": {"hit_rate@3": 0.87, "mrr": 0.80, "优势": "切分合理", "劣势": "实现复杂"}
}
```

#### 数据管道监控指标

```
管道健康度监控：
├── 数据新鲜度：上次成功更新时间
├── 管道成功率：成功执行次数 / 总执行次数
├── 数据量趋势：每日新增记录数
├── 检索质量趋势：Hit Rate@3 的变化
└── 存储使用量：ChromaDB 磁盘占用
```

### 7.4 数据工程版简历写法（扩展版）

> **智能数据管道与RAG检索系统** | ChromaDB / Embedding模型 / LangChain / Python
>
> 构建一套完整的数据处理与语义检索管道，覆盖从原始数据接入到结构化存储的全流程：
>
> - **数据管道**：设计基于Python的数据清洗与ETL流程，支持CSV/JSON多格式数据接入，自动完成字段类型推断、缺失值检测与处理、异常值识别
> - **向量化存储**：使用ChromaDB构建向量数据库，对比4种文档分块策略（chunk_size=256/512/1024 + 语义分块），选定512+128为最优方案（Hit Rate@3=89%, MRR=0.82）
> - **检索评估**：构建100+条QA对的评估数据集，对比text-embedding-3-small与bge-large-zh等Embedding模型在业务数据上的检索效果差异
> - **质量监控**：建立数据质量报告自动生成机制，覆盖缺失率、重复率、异常值检测等6个维度
>
> *关键词：RAG Pipeline、ChromaDB、数据质量、Embedding、向量检索、分块策略*

### 7.5 数据工程岗专属面试问答

- [ ] **「你这个管道处理多大的数据量？性能怎么样？」**
  > 目前是单机版，处理万级到十万级数据没问题。主要瓶颈在Embedding的API调用耗时。如果数据量到百万级，会换FAISS做向量检索，数据处理部分会用DuckDB或Polars替代Pandas。

- [ ] **「Chunk策略怎么验证的？除了Hit Rate还看了什么？」**
  > 建了100+条人工标注的QA对，主要看Hit Rate@3和MRR。另外也人工抽检了50条case，检查检索回来的内容是否真的对回答问题有帮助——有的case虽然命中率高但检索到的信息质量差，这说明chunk策略还要调。

- [ ] **「数据质量监控具体监控什么？阈值怎么设？」**
  > 监控6个维度：缺失率、重复率、异常值、数据类型一致性、数据新鲜度、管道成功率。阈值是根据业务数据的特点动态算的——比如缺失率用"均值+3σ"做动态阈值，而不是固定一个数字。

- [ ] **「这个管道的生产环境部署方案是什么样的？」**
  > 目前是单机版。生产化需要做三件事：数据处理用Airflow做定时调度、向量库从ChromaDB换Milvus（支持分布式）、代码执行用Docker沙箱隔离。数据量上来后还要加数据分区策略。

- [ ] **「Embedding模型选型怎么做的？对比了哪些？」**
  > 对比了text-embedding-3-small（1536维，OpenAI）、text-embedding-3-large（3072维）、bge-large-zh（1024维，中文）。在中文+英文混合场景下，3-small性价比最高，bge在纯中文场景更好。

---

## 八、项目亮点速查

### AI应用岗视角

| 模块 | 技术亮点 | 面试能讲的点 |
|------|---------|------------|
| A. 数据理解 | 结构化数据信息抽取 + LLM自然语言生成 | 你懂得怎么让LLM理解结构化数据 |
| B. Code Interpreter | 代码自动生成 + 执行 + 错误重试机制 | 你解决了AI代码执行中最头疼的"报错"问题 |
| C. RAG知识库 | ChromaDB + Embedding + 语义检索 | 你完整走过RAG全链路：分块→向量化→检索→生成 |
| 整体 | 提示词工程 | 你为每个模块设计了专门的Prompt策略 |

### 数据工程岗视角

| 管道环节 | 技术内容 | 面试能讲的点 |
|---------|---------|------------|
| 数据接入 | 多格式解析 + 字段类型推断 + 缺失值检测 | 你懂数据质量保障的基本方法 |
| 数据处理 | 自动化ETL + 异常值识别 + 统计摘要生成 | 你能设计可复用的数据处理流程 |
| 数据存储 | ChromaDB向量化 + 分块策略对比 + 持久化 | 你做过向量数据库的工程化落地 |
| 数据检索 | 语义检索 + 评估指标（Hit Rate/MRR） | 你不仅有实践还有评估方法论 |
| 监控 | 数据质量报告 + 管道健康监控 | 你有生产环境的思维，不是一个demo就完事了 |
