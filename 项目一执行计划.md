# 项目一执行计划（RAG 智能数据分析助手）

> 定位：AI 应用方向**主力项目**（亮点 ★★★★★），面试可讲 20 分钟
> 项目文档：[[项目一_智能数据分析助手]]（含模块 D 设计与简历版本三）
> 排序依据：[[../../求职准备/项目执行计划|AI 应用方向项目执行计划]] · 采纳结论见 [[../项目优化建议#采纳结论（2026-08-05 与 Claudian 探讨后）|项目优化建议]]

---

## 基础版任务（模块 A/B/C + 部署）

> 从零开始写代码，跑通全部功能，对照项目文档输出预期。

- [ ] 环境准备：安装 langchain / chromadb / openai / streamlit（复用 da-venv）
- [ ] 模块 A 数据理解：CSV 上传 → 数据特征提取 → LLM 生成数据概览
- [ ] 模块 B 代码执行：分析代码生成 + exec 执行 + 错误重试（≤3 次）
- [ ] 模块 C 向量知识库：ChromaDB 持久化存储 + 语义检索 + RAG 问答
- [ ] Streamlit 界面 + Streamlit Cloud 部署上线（环境变量 Secrets 配置）

## 升级点（内嵌，全部做）

### 模块 D 评估闭环（数据飞轮核心）

- [ ] **D1 离线评估集**：构建 XX 条 QA 对（覆盖 N 个分析场景），人工标注标准答案；指标 = Hit Rate@3 / MRR
- [ ] **D2 在线监控**：空结果率 / 失败率 / 用户负反馈 / 输出长度漂移 + 告警阈值（如连续 1 小时空结果率 >20%）
- [ ] **D3 Bad Case 迭代**：失败案例语义聚类归因（分块不合理 / 知识缺失 / Prompt 边界）→ 针对性优化 → 重跑评估集 → 对比沉淀（XX 轮，准确率 XX% → XX%，真算后填）

### 模块 E 多智能体架构（2026-08-06 内嵌，替代独立项目三方案）

> 决策与理由见 [[../../AI应用方向/项目优化建议#待执行计划（2026-08-05 登记）|项目优化建议 · 待执行计划]]；定调原则：「两个纵深项目 > 多个平均项目」
> 详细设计已并入主体文档 [[项目一_智能数据分析助手]]（模块 E 章节），任务清单见下方 E1-E5
> 定位：模块 A/B/C 之上加编排层，系统从「RAG 应用」升级为「多智能体架构应用」（Orchestrator-Workers，Anthropic 官方多智能体设计模式）；简历标题保留「多智能体」关键词

- [ ] **E1 Orchestrator 编排层**：任务拆解 / 分派策略 / 结果汇总与仲裁（优先级规则 / 阈值）/ 回退重试（≤N 次）/ 防死循环（最大轮次上限 + 超时）
- [ ] **E2 Worker 化改造**：模块 B（代码执行）、模块 C（知识库检索）改造为独立 Agent（独立 System Prompt + 工具集）；新增审校 Worker（结果完整性检查 + 兜底回答）
- [ ] **E3 LangGraph 状态图**：路由→分派→各 Worker→重试→汇总→审校，状态传递与日志（面试可讲的状态管理细节）
- [ ] **E4 评估扩展**：离线评估集加多智能体场景 QA 对（复杂多步任务、拆解正确性标注）；指标 = 工具调用成功率 / 任务完成率 / 分派准确率 / 重试率 / 死循环率；bad case 迭代（XX 轮，真算后填）
- [ ] **E5 简历叙事**：标题带「多智能体」，正文「问题+解决+量化指标」（XX 占位，真算后回填）

### 叙事与沉淀

- [ ] 简历版本三「数据飞轮版」数字回填（XX 占位 → 真算值）
- [ ] 副产品：开发中每次调 Prompt 记录实验（输入 / 输出 / 结论）→ 沉淀为副产品素材（原项目三降级，不占简历位）

## 面试必答

### 技术类（项目文档第六节）

- [ ] 为什么用 LangChain 而不是直接调 API？
- [ ] Chunk 策略怎么设计的？为什么？（512+128 的依据要能说）
- [ ] Code Interpreter 的安全性怎么处理？（exec + 超时 + 重试，生产要沙箱）
- [ ] Embedding 模型为什么选 text-embedding-3-small？
- [ ] 遇到的困难：AI 生成代码报错怎么解决？（重试机制成功率 XX%→XX%，真算）

### 多智能体类（2026-08-06 新增，模块 E 配套）

- [ ] 任务怎么拆解的？为什么这么拆？（Orchestrator 设计依据，真设计）
- [ ] 专家结果冲突怎么仲裁？（优先级规则 / 阈值 / 人工介入）
- [ ] 工具调用失败怎么处理？重试机制成功率 XX%→XX%（真算）
- [ ] 怎么防死循环？（最大轮次上限 + 超时 + 监控告警）
- [ ] 你的场景为什么需要多智能体而不是单智能体？（对照 Anthropic「从简」原则能自圆其说）

### 数据飞轮类（新增 4 问）

- [ ] 评估集怎么构建的？多少条、覆盖哪些场景？
- [ ] 监控指标为什么选这几个？阈值怎么定？
- [ ] Bad Case 怎么聚类归因？举一个真实迭代案例
- [ ] 迭代几轮、提升多少？（真数字）

## 收尾

- [ ] 数字吃透验收：每个真算数字能答三问——怎么算出来的（代码哪一行）/ 为什么这么算（口径）/ 在故事里干什么用
- [ ] 面试官模拟：请 Claudian 当面试官考一轮（对齐数据分项目一模式）
- [ ] 简历数字核对：真算数字回填 AI 版简历草稿，删除参考值
- [ ] 知识点记录：实现中的疑问记入「待问清单」，清零后再进下一步（项目二）

## 简历数字红线

- ✅ 可写：真算出来的——Hit Rate@3 / MRR、重试成功率、迭代轮次与准确率提升（XX%→XX%）
- ❌ 暂不写：专家参考值（75%→92%、76%→91%、89% 等）——项目未实现、数字无真实出处，实现后真算才能写
