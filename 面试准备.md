# AI 数据分析助手 · 面试准备

---

## 一、一句话项目定位

> **"面向数据分析师的智能分析加速器。不是替代分析师做结论，而是加速 EDA、检测盲点、评估可信度——让分析师专注于 AI 做不了的因果推断和业务解读。"**

---

## 二、核心差异化（一句话版）

> "大多数 AI 分析工具默认假设'AI 的结论是对的'，我的系统默认假设'AI 的结论需要被质疑'。"

---

## 三、面试必问 7 题

### 1. "你这东西给谁用？和市面上其他工具有什么区别？"

> 给数据分析师用的。不是想做"一键出报告替代人"，而是想做"分析师的 AI 搭档"。区别在于——其他工具只输出结论，我的系统会主动标注哪些结论可信、哪些需要验证、哪些角度还没分析。七板块里最核心的是🎯可信度评估和🧠盲点检测，其他工具都没有。

### 2. "这不就是用 LangGraph 搭了个 Demo 吗？"

> LangGraph 只是编排层的实现工具。这个系统的核心不是用哪个框架，而是 Agent 之间的协作协议设计——Orchestrator 有三层仲裁：检查任务能力范围、监控执行时间和输出长度、对连续相同输出做死循环检测。这些异常处理在 Demo 里看不出来，但在生产环境是必须的。

### 3. "你和 Jupyter Notebook 有什么区别？"

> Notebook 是工具，我的系统是搭档。Notebook 不会主动告诉你"这部分还没分析"或者"这个结论存在辛普森悖论的风险"。我的系统会。它做的事是分析师在复盘时才做的事——检查自己有没有遗漏、有没有被数据欺骗。

### 4. "你生成的分析结果，怎么保证是正确的？"

> 两层验证。第一层代码级：AnalysisWorker 生成的 Python 代码先在沙箱执行，ReviewWorker 检查结果格式和合理性，不通过打回重试。第二层统计级：ReviewWorker 检查样本量是否足够、是否存在混淆变量、结论是否暗示了因果但实际只是相关性。虽然目前还是规则+LLM 判断，但这套验证框架的设计思路源于我的数据科学训练。

### 5. "可信度评估有没有判错过？"（准备好翻车案例）

> 当然有。比如上一条分析报告里，第五条"折扣换量效果有限"被标了🔴——系统自己承认这个结论可能不对，因为没有交叉分析证据，且未控制订单量、客单价等混淆变量。这恰恰证明了这个系统的价值：它会质疑自己给出的结论。

### 6. "你这在生产环境能跑吗？"

> 目前是本地 Demo。但架构上预留了三个生产扩展点：一是 Agent 和 LLM 解耦，可切换私有化部署模型；二是 Orchestrator 调度逻辑独立于 Worker，增加新分析能力不需要改编排层；三是代码沙箱限制网络和文件系统访问。这三个设计让企业级落地不需要推倒重来。

### 7. "为什么要做可信度评估和多源交叉验证？"

> 这源于我的数据科学背景。在统计学里，我们从不只看一个数字就下结论，而是要做敏感性分析和稳健性检验。我把这个思维方式工程化了——系统会自动检查样本量、混淆变量、因果推断风险，低可信度时自动触发替代验证。这个设计的灵感来自统计学的实验设计方法论。

---

## 四、面试演示脚本（8 分钟）

| 时间 | 做什么 | 说什么 |
|---|---|---|
| 0-1min | 上传 sample_ecommerce.csv | "这是一个模拟电商数据集，200 天 x 7 列" |
| 1-3min | 点一键分析，等进度条跑完 | 介绍进度条含义："系统在并行执行图表生成、分析报告、可信度评估、替代验证" |
| 3-5min | 展示 7 板块报告 | 重点停在🎯可信度评估："看，这里有 🔴——系统自己承认这个结论可能不对" |
| 5-6min | 点追问按钮，"按周度拆解销售趋势" | 展示盲点→追问→新分析的完整闭环 |
| 6-7min | 被追问架构时，讲多 Agent 协作 | "Orchestrator-Workers，四步流水线，这个设计在项目一的 Agent 研究里有更详细的对比" |
| 7-8min | 自然过渡到项目一 | "这个系统的 Agent 架构设计思路，源于我对 LangGraph/Pi Agent/Claude Code 三个框架的源码分析..." |

---

## 五、防翻车检查清单

- [ ] 提前准备一份会让系统出 🔴 的"脏数据"（含生存偏差、辛普森悖论），现场演示
- [ ] 提前准备一个会让 Agent 失败的问题（问数据里没有的变量），展示降级输出
- [ ] 确保每个数字都能说出计算口径
- [ ] 确保每个技术决策都能解释"为什么"
- [ ] 不要用术语堆砌——面试官可能不是你同方向的
- [ ] 被问到"这个数字怎么来的"，直接答代码逻辑，不绕弯

---

## 六、关键数字速记

| 项目 | 数字 |
|---|---|
| 样本量 | 200 天模拟电商数据 |
| 分析板块 | 7 个（发现、质量、建议、方向、盲点、可信度、替代验证） |
| Agent 数量 | 4 个 Worker（Retrieval / Analysis / Executor / Reviewer）|
| 沙箱保护 | FORBIDDEN 关键词拦截 + ast 语法预检 |
| 重试机制 | 代码错误自动发回 LLM 修正，最多 3 次 |
| 替代验证 | 可信度低于 🟢 时自动触发 |
| 模型 | DeepSeek chat API |

---

## 七、"不是 Toy" 的核心论据

1. 异常处理：重试 ≤3 次 + 防死循环 + 连续相同输出检测
2. 沙箱安全：危险函数拦截 + ast 语法预检
3. 对话记忆：ChromaDB 向量库存储历史结论（已设计，网络限制暂未启用）
4. 评估闭环：可信度评分 + 替代验证 + 盲点检测
5. 状态管理：LangGraph 状态机，Orchestrator 管理任务状态和仲裁

---

## 八、和项目一（Agent 研究）的协同话术

> "我理解您的担心。实际上，这个数据分析助手是我对 Agent 架构研究的一个应用验证。我在项目一里深入分析了 LangGraph 的源码，研究了它的状态管理、任务调度和错误恢复机制，然后自己写了一个极简框架来验证我的理解。这个助手就是把这些研究成果应用到一个真实场景里。所以它不仅是能跑，它的每一步调度逻辑我都能解释清楚为什么这样设计。"

---

## 九、专家补充：三大质疑场景回击

### 场景一："这不就是用 LangGraph 搭了个 Demo？"

> LangGraph 只是编排层的实现工具。这个系统的核心不是用哪个框架，而是我怎么设计 Agent 之间的协作协议。比如 Orchestrator 收到分析请求后，不是简单地把任务丢给 Worker 然后等结果。它有三层仲裁逻辑：第一层检查任务是否在 Worker 的能力范围内，不在就降级给用户；第二层监控执行时间和输出长度，超时或异常自动回退重试；第三层对连续三轮相同输出做死循环检测。这些异常处理逻辑在 Demo 里看起来没什么用，但在实际生产环境是必须的。我项目二的框架研究，就是围绕这些生产级问题做的。

### 场景二："数据分析工具太多了，你这有什么特别的？"

> 大多数 AI 分析工具的默认假设是"AI 给出的结论是正确的"。我的系统默认假设是"AI 给出的结论需要被质疑"。所以核心设计不是分析能力本身，而是可信度评估和盲点检测。每个结论必须被 ReviewWorker 打分，低可信度的结论会被标注出来——你刚才看到报告里那个 🔴，就是系统自己承认"这个结论可能不对"。这不是功能差异，是设计哲学差异。

### 场景三："你这个在生产环境能跑吗？"

> 目前是本地单文件版，面向 Demo 场景。但我在架构上预留了生产扩展点：一是 Agent 和 LLM 后端解耦，可以切换到私有化部署模型解决数据安全问题；二是 Orchestrator 的调度逻辑独立于 Worker，增加新分析能力不需要改编排层；三是代码执行在沙箱里，限制网络和文件系统访问，这是生产环境代码生成类功能的基本安全保障。如果需要企业级落地，这三个设计可以让我不用推倒重来。

---

## 十、专家补充：企业级落地思维（面试加分项）

面试时主动提以下四点，证明你不是只会做玩具项目：

**1. Skills封装 vs 全自动探索**
> 我现在的设计偏全自动探索。但如果面向企业级场景，我的 Agent 架构可以很自然地切换到 Skills 模式——把每个分析步骤封装成独立 Worker，Orchestrator 统一调度。这恰好是我项目一研究的 Agent 编排能力的直接应用。

**2. 数据安全与合规**
> 目前项目是本地单文件上传。如果面向企业级，有两个关键升级：一是数据不离开本地，用本地部署的 LLM；二是对接数仓，通过 Skills 封装取数逻辑而不是把原始数据丢给 AI。这些在 Agent 架构里留了扩展接口。

**3. 因果推断是 AI 的死穴**
> LLM 本质上是概率统计，搞的是相关性最高。但数据分析有大量假设和论证都是在做因果。我的系统在可信度评估里专门加了"因果推断风险"维度——如果结论暗示"A 导致 B"，系统会自动核查是否仅观察到相关性，观察性数据自动降一级可信度。这源于小红书上多位从业者的共识：AI 替代不了数据分析师的核心原因是因果推断。

**4. 分析盲点引导人机协作**
> 系统在盲点检测中主动声明"未纳入业务背景因素"。这有三个用意：主动承认 AI 的局限、引导人机协作、展示我知道"数据分析的本质是业务理解，不只是跑数"。

---

## 十一、专家补充："翻车演示"脚本

面试中最高级的技巧：**主动展示系统的失败案例。**

准备一份"脏数据"（含生存偏差、有明显辛普森悖论的汇总表），上传后系统会给出 🟡 或 🔴 的可信度结论。指着屏幕说：

> "你看，这里被我预埋了一个辛普森悖论，系统注意到了样本量集中在某个子集，自动降了可信度。这个结论我自己也不信——这恰好证明了这个系统的价值：它会质疑自己给出的结论。"

同时也准备一个会触发 Agent 退出的问题（问数据里根本没有的变量）。演示中 Orchestrator 尝试分派 → 报错 → 自动发回修正 → 三次重试失败 → 优雅降级，输出"无法完成此分析，建议检查变量名"。指着日志讲出每个 Agent 干了什么、Orchestrator 做了什么仲裁决策。

---

## 十二、专家补充：数据分析师工作流嵌入

**数据分析师真实工作流，你的系统如何嵌入：**

```
拿到需求
  ↓
① 数据清洗                   ← AI 可以加速
  ↓
② 探索性分析                  ← 系统提供"初版7板块报告"
  ↓
③ 发现疑点 → 深挖             ← 追问对话在这里
  ↓
④ 验证假设                    ← 替代验证+可信度评估在这里
  ↓
⑤ 写报告/做PPT                ← Markdown 导出在这里
```

**系统不是替代 ②③④，而是加速 ② → 提供 ③ 的起点 → 提醒 ④ 不要漏。**

面试时可以指着这个工作流说："我的系统定位非常清晰——它解决的是 EDA 加速和盲点检测，不是替代分析师做最终决策。"

---

## 十三、分析结论如何对接业务部门（面试高频）

**面试官问"你分析出来的东西，业务部门看得懂吗？"**

> 我的系统定位是分析师的加速器，不是替代分析师去跟业务沟通。系统出的是技术层面的分析——可信度评分、盲点检测、替代验证。分析师拿到报告后，把发现翻译成业务语言。

> 比如系统说"折扣率与销售额相关性不显著，可信度🔴"，分析师对业务说的是"咱们的折扣活动效果不太确定，建议做一次 A/B 测试验证"。这个翻译工作，AI 做不了，是分析师的核心价值。

> 但系统能帮分析师做得更好：盲点检测让分析师知道跟业务汇报时哪些地方要打预防针；可信度评估让分析师知道哪些结论可以自信地说、哪些要留余地；追问对话让分析师在业务方提出质疑时能快速拿数据验证。

---

## 十四、Temperature=0 的设计决策（可复现性 vs 分析多样性）

**面试官可能问"同样的数据跑两次，结论一定一样吗？"**

> 我的系统设了 temperature=0，确保分析结论可复现。这在数据分析场景里非常重要——你不能上周和这周拿同样的数据得出不同的结论。但我意识到这里有一个权衡：temperature=0 意味着 LLM 每次都走同一条分析路径，可能错过其他有价值的分析角度。

> 如果面向生产环境，我会用混合策略：事实性分析（数据发现、质量问题、可信度评估）用 temperature=0，保证可复现；观点性分析（业务建议、下一步方向、盲点）用 temperature=0.3，允许多样化的洞察。然后用户可以选择"重新探索"来获得不同角度的建议。这个设计思路源于数据科学的敏感性分析——看看换一个分析路径结论是否稳健。

---

## 十五、一句话终极定位

> **"比全自动工具更有审慎思维，比企业级方案更轻量可演示。能展示你比数据分析师更懂 AI、比 AI 工程师更懂分析的桥梁型项目。"**

---

## 十七、专家补充：图表交互细节设计原则

**每个交互细节，要么满足分析师的本能（如 df.head()），要么让 7 板块结论更有说服力。不做纯装饰的交互。**

图表和结论的对应关系：

| 报告结论 | 需要的图 | 交互细节 |
|---|---|---|
| 销售规模稳定但存在波动 | 日销售额+订单量双轴折线图 | 时间粒度切换、悬停数值 |
| 退货率存在长尾风险 | 退货率趋势图+阈值线 | 阈值线可拖拽、超阈值点标红 |
| 折扣率与销售额关系值得警惕（🔴可信度） | 折扣率 vs 销售额散点图+趋势线 | 气泡大小=订单量、悬停日期 |
| 新客占比偏低且波动大 | 新客占比周趋势折线图+参考线 | 粒度切换、中位数参考线 |
| 多变量关系 | 相关性热力图 | 点击格子生成散点图 |

散点图是整个报告里最重要的图——因为可信度评估里唯一标 🔴 的就是折扣率相关结论。面试时指着散点图说"高折扣并没有对应高销售额——这就是我标 🔴 的原因，但我没有直接采信这个肉眼判断，而是标注了需要做回归验证"。

---

## 十八、数据安全与隐私（面试高频问题）

**面试官问"你的数据安全吗，公司数据能给你分析吗？"**

> 当前版本是本地运行的单用户 Demo，原始数据不上传云端，安全风险可控。但如果面向企业级场景，我会做三层防护。第一层，数据传输：部署本地 LLM 或使用私有化 API，确保分析请求不出企业内网。第二层，数据脱敏：在数据上传后自动检测身份证号、手机号、银行卡号等敏感字段，用掩码处理后再进入分析流程。第三层，权限隔离：不同角色的分析师看到不同的数据粒度——比如区域经理只能看自己区域的数据，不能跨区查询。

**面试官追问"AI 生成的代码安全吗？"（exec() 沙箱）**

> 代码执行安全我做了四层防护。第一层，ast 语法预检，拒绝包含 subprocess、shutil.rmtree 等危险关键词的代码。第二层，IO 隔离，stdout/stderr 全部捕获，不直接输出到系统。第三层，超时控制，代码执行超过 30 秒自动终止。第四层，重试上限，报错最多重试 3 次，避免死循环。这四层确保 AI 生成的代码不会对用户系统造成破坏。

---

## 十九、专家补充："换数据怎么办"标准回答

面试官问"换一份数据你的系统还能用吗？"

> 我这个系统是面向数据分析师设计的探索加速器。图表类型和交互逻辑是预设的分析模板，LLM 的角色是自动识别数据类型和目标变量，然后匹配对应的模板。这样做有两个好处：一是行为可预期，不会因为 LLM 的随机性生成奇怪的图表；二是分析师的交互体验是一致的，他们不用每次换数据都重新学习界面。如果换成企业级场景，每个公司可以定制自己的分析模板。

核心逻辑：LLM 负责"识别数据类型 → 套模板"，不是"设计交互细节"。面试官换数据，图会变，但图的类型和交互逻辑不变。

---

## 二十、列类型推断——为什么不做纯规则过滤

**面试官问"异常值检测误判ID列怎么办？"**

> 我设计了一个列类型推断模块，在异常检测之前先判断每一列是什么类型——🆔ID列、🏷️分类编码列、🔘布尔列、📅日期列、📊连续指标列。不同类型对应不同的质量检测逻辑。ID列不跑异常值检测，检查唯一值覆盖率；分类列检查类别分布；只有真正的连续指标列才跑IQR。

> 这个设计的核心思想：数据质量检测不能一视同仁。不同语义的列需要不同的检测策略。列类型推断就是把"语义理解"做在"统计检测"之前。不是用规则层层过滤误判，而是从源头上区分每列该做什么检测。
